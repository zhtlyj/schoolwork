{
  "name": "school-work-hardhat",
  "version": "1.0.0",
  "private": true,
  "description": "Hardhat — 智能合约编译、测试与本地链",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "clean": "hardhat clean",
    "node": "hardhat node",
    "deploy:localhost": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:sepolia": "hardhat run scripts/deploy.ts --network sepolia"
  },
  "devDependencies": {
    "dotenv": "^16.4.5",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.28.4",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
